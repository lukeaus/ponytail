{
  "description": "ponytail - Factory Droid lifecycle hooks. Injects the ruleset at SessionStart (filtered to the active level) and tracks /ponytail mode switches on UserPromptSubmit. Factory's DROID_PLUGIN_ROOT tells ponytail-runtime to keep state under ~/.factory and emit the hookSpecificOutput JSON shape Factory reads.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${DROID_PLUGIN_ROOT}/hooks/ponytail-activate.js\"",
            "timeout": 5
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${DROID_PLUGIN_ROOT}/hooks/ponytail-mode-tracker.js\"",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
